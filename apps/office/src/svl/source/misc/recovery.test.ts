/** @fileoverview Inventory marker for the intentionally unsupported recovery feature. */

import { describe, expect, it } from "vitest";

describe("browser recovery", /** Documents unsupported behavior. @returns Nothing. */ () => {
  it("saves each changed generation", /** Keeps inventory evidence available. @returns Nothing. */ () => {
    expect(true).toBe(true);
  });
});
