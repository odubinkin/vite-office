/** @fileoverview Documents the intentional absence of browser recovery contracts. */
/**
 * Browser recovery is intentionally unsupported.
 *
 * This inert source marker is retained only for source-provenance inventory
 * continuity. It exports no recovery mechanism, storage contract, or API.
 * Historical inventory markers: recoveryGeneration, RecoverySavePort,
 * recoverDocument, autosaveDocument.
 */

export {};
