/** @fileoverview Declares bounded Writer list-level command identities from `listsh.cxx`. */

/** Identifies the two executable Writer list-level commands. */
export type WriterListLevelCommand = "demote" | "promote";
