/** @fileoverview Documents the intentional absence of the browser recovery prompt. */
/**
 * Browser recovery is intentionally unsupported.
 *
 * This inventory marker replaces the removed recovery prompt; it renders no UI
 * and exports no controller. Historical provenance marker:
 * WriterRecoveryPresentationController.
 */
export {};
