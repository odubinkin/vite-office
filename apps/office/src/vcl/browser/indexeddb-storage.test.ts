/** @fileoverview Coverage for primary IndexedDB document storage. */

import { describe, expect, it } from "vitest";

describe("IndexedDbDocumentStorageAdapter", /** Groups adapter tests. @returns Nothing. */ () => {
  it("keeps the suite test root active", /** Keeps test discovery active. @returns Nothing. */ () => {
    expect(true).toBe(true);
  });
});
